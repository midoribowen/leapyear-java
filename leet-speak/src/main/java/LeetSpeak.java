public class LeetSpeak {
  public static void main(String[] args) {

    public String leetSpeak(String userInput) {
      String returnString = new String();

      for ( int i = 0; i < str.length(); i++ ) {
        if ( userInput.charAt(i) == 'e' || 'E') {
          returnString += "3";
        } else {
          returnString += userInput.charAt(i);
        }
        }
      }
      return userInput.charAt();
    }

  }
}
